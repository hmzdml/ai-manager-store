{
  "name": "shopbot-backend",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "migrate": "prisma migrate dev",
    "build": "tsc"
  },
  "dependencies": {
    "axios": "^1.5.0",
    "@prisma/client": "^5.0.0",
    "express": "^4.18.2",
    "dotenv": "^16.0.0",
    "pino": "^8.12.0"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "ts-node-dev": "^2.0.0",
    "prisma": "^5.0.0"
  }
}
